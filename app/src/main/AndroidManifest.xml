<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.control_estacionamiento_utec_electiva_i">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Estudiante.StudentRegisterActivity"></activity>
        <activity
            android:name=".Admin.MainActivityAdmin"
            android:label="@string/title_activity_navegation_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Estudiante.EstudiantesDrawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Vigilante.Detalle" />
        <activity
            android:name=".Vigilante.vigilanteNavigationDrawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".docentes.DocenteHome"
            android:label="@string/title_activity_docente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".docentes.Estacionamiento" />
        <activity
            android:name=".Login.Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>